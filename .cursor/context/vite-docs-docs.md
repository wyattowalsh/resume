# Project Context Note

This file previously contained a packed copy of upstream Vite documentation and incorrectly described itself as a packed representation of this repository.

Use the current project docs instead:

- `README.md` for routes, commands, artifact policy, and public downloads.
- `docs/content/docs/index.mdx` for the documentation site overview.
- `.cursor/codebase.txt` for a concise Cursor-oriented project summary.

This resume project uses Vike + React, canonical data in `assets/data/resume.json`, variant overlays in `assets/data/variants/`, and artifact generation under `src/scripts/`.
