name: Actions Smoke

on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  smoke:
    name: Start a GitHub-hosted runner
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Report runner
        run: echo "GitHub Actions runner started"
